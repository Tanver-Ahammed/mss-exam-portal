package com.mss.exam.portal.entity.enums;

/**
 * Classifies every stored file in the system.
 */
public enum FileType {
    PROFILE_PICTURE,
    SUBMISSION_PDF,
    SUBMISSION_IMAGE,
    CERTIFICATE,
    EXAM_ATTACHMENT
}
